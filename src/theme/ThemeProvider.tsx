// This file is no longer needed as MUI handles theme context internally
// Use the useColorScheme hook directly from @mui/material/styles
// Use the ThemeSwitcher component for theme switching

export { useColorScheme as useTheme } from '@mui/material/styles';
